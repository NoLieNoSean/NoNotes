A group is a system or collection of actions satisfying these rules
- There is a predefined list of actions that never changes
- Every action is reversible
- Every action is deterministic
- Any sequence of consecutive actions is also an action.

A generator set is a set of "actions" which generate all other actions

---

The notion that performing actions change the POV. For example, flipping in the dihedral group.


Cayley's theorem: Any group can be built(?) from permutation groups.

---
$\mathbb{Z}_{mn}\cong \mathbb{Z}_{m}\times \mathbb{Z}_{n}$ when $(m, n)=1$.

If $gH=Hg$ for some $g\in G$, is it true for all $g\in G$?

---

Factors in any direct product group are normal subgroups

